name: credit_trak
description: Application mobile de gestion des transferts et crédit.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  uuid: ^4.0.0
  intl: ^0.19.0
  pdf: ^3.10.0
  path_provider: ^2.1.0
  blue_thermal_printer: ^1.2.3
  fl_chart: ^0.66.0
  google_fonts: ^6.0.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
